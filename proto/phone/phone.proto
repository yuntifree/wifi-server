syntax = "proto3";

package go.micro.srv.phone;

service Phone {
    rpc GetCode(GetRequest) returns (GetResponse) {}
}

message GetRequest {
    string phone = 1;
    int64 uid = 2;
}

message GetResponse {
}
